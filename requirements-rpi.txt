# PQC Benchmarks — Raspberry Pi 5 dependencies
# Python 3.11+ required
# Run: pip install -r requirements-rpi.txt

# ── Analysis stack (same as requirements.txt)
-r requirements.txt

# ── RPi 5 hardware — not installable on standard machines
# INA219 current sensor (I2C)
adafruit-circuitpython-ina219==3.4.4
adafruit-blinka==8.20.1

# GPIO
RPi.GPIO==0.7.1

# System metrics
psutil==5.9.5

# liboqs Python wrapper — requires liboqs compiled for ARM64
# Installation: https://github.com/open-quantum-safe/liboqs-python
oqs==0.9.0

# Classical cryptography (RSA, ECC)
cryptography==41.0.3
